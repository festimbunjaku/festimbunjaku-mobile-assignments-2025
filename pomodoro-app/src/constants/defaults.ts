export const DEFAULT_SETTINGS = {
  WORK_DURATION: 30, // minutes
  BREAK_DURATION: 10, // minutes
  ALARM_SOUND_ENABLED: true,
  DARK_MODE_ENABLED: false,
};

export const TIMER_CONSTANTS = {
  MIN_WORK_DURATION: 1, // minutes
  MAX_WORK_DURATION: 60, // minutes
  MIN_BREAK_DURATION: 1, // minutes
  MAX_BREAK_DURATION: 30, // minutes
  TICK_INTERVAL: 1000, // milliseconds (1 second)
};

export const HISTORY_CONSTANTS = {
  DAYS_TO_FETCH: 30,
  ITEMS_PER_PAGE: 50,
};

export const STATS_CONSTANTS = {
  GRAPH_DAYS: 7,
};
